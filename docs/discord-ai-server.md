# Discord AI (Unrestricted Server)

## Description
Unrestricted Node.js AI Discord bot server that runs locally with TLS certificate bypass for self-signed certifications.

## Usage (Windows PowerShell)
```powershell
$env:NODE_TLS_REJECT_UNAUTHORIZED="0"
node server.js
```
